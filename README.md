# rainOS Tablet

A clean tablet-first rainOS simulation built from scratch with React and Vite.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The project intentionally keeps the foundation small and dependency-light so the tablet UI can grow without inheriting the mobile project's architecture or compatibility issues.
